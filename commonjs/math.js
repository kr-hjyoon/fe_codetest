const perfectScore = 100;

const sum = (num1,num2) => {
    return num1 + num2;
}

const avg = (num1,num2) => {
    return (num1 + num2)/2;
}

const substract = (num1,num2) => {
    return num1 - num2;
}

module.exports = {
    perfectScore,
    sum,
    avg,
    substract
}
